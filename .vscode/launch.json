{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Package",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}", // This will debug the main package in your workspace root
            "env": {},
            "args": []
        }
    ]
}